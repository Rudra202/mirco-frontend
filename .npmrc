# pnpm configuration — controls dependency resolution behavior.

shamefully-hoist=true          # Hoist all dependencies to root node_modules (flat layout)
strict-peer-dependencies=false # Allow missing peer dependencies without error
auto-install-peers=true        # Automatically install peer dependencies
