// ---------------------------------------------------------------------------
// Shell App — PostCSS Configuration
// Processes CSS with Tailwind CSS and Autoprefixer.
// ---------------------------------------------------------------------------
module.exports = {
  plugins: {
    tailwindcss: {},   // generate utility classes from Tailwind config
    autoprefixer: {},  // add vendor prefixes for cross-browser support
  },
};
